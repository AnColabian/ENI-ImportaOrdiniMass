# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: importaordinivendita
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: BAS_RD6_DEV_SITAM4_HTTPS
          url: http://rd6sitam4dev:443
          client: '100'
        app:
          name: ZIMPORTODVEXCEL
          package: ZFIORI
          transport: RD6K908839
        exclude:
          - /test/
